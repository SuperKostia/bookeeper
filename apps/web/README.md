# @bookeeper/web

Next.js 15 (App Router). Marketing site + admin back-office.

## Dev

```bash
pnpm -F @bookeeper/web dev
# http://localhost:3000
```

## Statut

Placeholder pour l'instant. Le vrai site marketing remplacera `index.html` à la racine du repo une fois prêt. Déploiement prévu sur Vercel.
